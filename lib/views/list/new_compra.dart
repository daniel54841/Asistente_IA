import 'package:flutter/cupertino.dart';

class NewCompra extends StatelessWidget {
  const NewCompra({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
